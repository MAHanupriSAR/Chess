#ifndef HELPER_FUNCTIONS_H // Header Guard: prevents double inclusion
#define HELPER_FUNCTIONS_H

#include <bits/stdc++.h>
using namespace std;

void setBit(uint64_t &value, int index);
bool getBit(uint64_t value, int index);
void popBit(uint64_t &value, int index);

#endif